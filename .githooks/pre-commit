#!/usr/bin/env zsh
set -euo pipefail

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

if ! git diff --cached --quiet -- . ':(exclude)VERSION'; then
  python3 scripts/versioning.py bump-local --stage >/dev/null
fi
