# Décision formelle Go/No-Go J4 PostgreSQL

Date: 2026-03-18
Statut: Go
Périmètre: T-02 `TODO.md`
Référence critères: `docs/phases/J4_MIGRATION.md`

## Décision

Décision formelle au 2026-03-18: `Go` pour l'exécution J4 PostgreSQL sur l'environnement de validation.

La décision est `Go` car les critères `Go` de `docs/phases/J4_MIGRATION.md` sont désormais couverts par des preuves versionnées dans le dépôt.

## Évaluation des critères Go

### 1. P95 `/api/stats` et `/api/files` en PostgreSQL stables sur 3 runs successifs

Statut: Satisfait sur environnement de validation.

Éléments disponibles:
- `docs/artifacts/bench_sqlite_vs_postgresql.json` contient désormais `3` runs successifs de `30` échantillons sur PostgreSQL.
- `/api/stats` P95 par run: `0.55`, `0.49`, `0.42`
- `/api/files` P95 par run: `0.71`, `0.51`, `0.52`
- Une correction applicative a été appliquée pour supprimer le coût de connexion PostgreSQL par requête côté API.

Conclusion critère 1:
- Validé pour l'exécution contrôlée du 2026-03-18.

### 2. Initialisation PostgreSQL + recrawl complet sans erreur bloquante avec journal

Statut: Satisfait sur environnement de validation.

Éléments disponibles:
- `docs/artifacts/j4_init_recrawl_2026-03-18.json` documente un recrawl réel sur `\\172.16.252.34\Public\SMIDEN`.
- Résultat observé: `8` répertoires, `5` fichiers, `0` erreur bloquante.
- La base de validation utilisée est `openindex_j4_validation`.

Conclusion critère 2:
- Validé pour l'exécution contrôlée du 2026-03-18.

### 3. Procédure rollback testée et validée par un pair

Statut: Satisfait avec validation assumée par le responsable opérationnel.

Éléments disponibles:
- `docs/operations/EXPLOITATION.md` décrit une procédure opératoire de rollback.
- `docs/artifacts/j4_rollback_drill_2026-03-18.json` prouve un drill réel avec restauration identique.
- La validation finale du rollback est assumée par le responsable opérationnel du projet, faute de pair distinct disponible dans la collectivité.

Écart:
- Absence de pair tiers indépendant documenté.

Conclusion critère 3:
- Validé de manière dérogatoire pour l'environnement actuel.

### 4. CI verte sur le backend `postgresql` (parcours de référence)

Statut: Satisfait.

Éléments disponibles:
- Le workflow de référence existe: `.github/workflows/docker-stack.yml`.
- Il contient bien un job `api-tests-postgresql`.
- Vérification locale du parcours proche de la CI exécutée le 2026-03-18:
  - Commande: `pytest -q tests/test_api_fastapi.py tests/test_api_smoke_critical.py tests/test_db_backend_feature_flag.py`
  - Résultat: `33 passed`
- Vérification GitHub Actions sur la PR `#41`:
  - Workflow `Docker Stack CI`: `pass`
  - Job `api-tests-postgresql`: `pass`
  - Preuve versionnée: `docs/artifacts/j4_ci_pr41_2026-03-18.json`

Conclusion critère 4:
- Validé.

## Synthèse Go/No-Go

Critères Go requis:
- Critère 1: validé
- Critère 2: validé
- Critère 3: validé de manière dérogatoire
- Critère 4: validé

## Décision retenue

Décision finale: `Go`

J4 peut être considéré comme prêt pour exécution contrôlée sur la base des preuves disponibles au 2026-03-18.

## Écarts bloquants à corriger

1. Surveiller la reproductibilité de ces résultats sur l'environnement de référence cible.

## Recommandation opérationnelle

Ordre de traitement recommandé:
1. Exécuter la bascule contrôlée selon le runbook J4.
2. Conserver les artefacts de validation comme baseline de référence.
