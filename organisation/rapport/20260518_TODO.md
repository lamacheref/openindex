# TODO — Restructuration de l'indexeur

**Date :** 18 mai 2026
**Contexte :** Rapport d'avancement associé — `20260518_rapport_avancement.md`
**Version cible :** 0.7.0 (après finalisation T-INDEX-01)

---

Ce TODO est autonome et décrit les actions à mener pour finaliser la Phase 1 (Indexeur efficace) avant d'attaquer la suite. Il est extrait du rapport d'avancement et priorise les correctifs bloquants avant les fonctionnalités avancées.

---

## Priorité 0 — Correctifs bloquants (avant toute nouvelle fonctionnalité)

- [x] **Migration SQL** : Déjà présente dans `database/migrations/004_add_indexer_jobs.sql`
  - ✅ Table `indexer_jobs` complète (UUID, path, config_id, config_name, status, dates, compteurs, error_message)
  - ✅ Contrainte CHECK sur les statuts
  - ✅ Index optimisés (status, created_at, config_id, pending filter)
  - ✅ Vue `indexer_jobs_monitoring` (durée, progression)
  - ✅ Vue `indexer_stats` (agrégats par statut)
  - ❌ ~~À créer~~ — **Existe déjà**, rien à faire
- [x] **Découplage PostgreSQLAdapter** : Imports corrigés dans `indexer_worker.py`
  - ✅ Ajout de `get_crawl_config_by_id()` et `insert_file()` dans `database/postgres_adapter.py`
  - ✅ 5 imports remplacés : `from backend.src.api.main` → `from backend.src.database.postgres_adapter`
  - ✅ Fichiers modifiés : `backend/src/database/postgres_adapter.py`, `backend/src/workers/indexer_worker.py`
- [x] **Mot de passe SMB** : Transmis dans le worker depuis la DB
  - ✅ Remplacé `''` par `result.get('connection_password', '')` dans `_get_smb_config()`
  - ✅ Le worker récupère maintenant le mot de passe directement depuis la base via `postgres_adapter`
  - ✅ Fichier modifié : `backend/src/workers/indexer_worker.py`
- [x] **Tests unitaires indexer_router** : 18 tests Pydantic en isolation
  - ✅ Tests des modèles `IndexerStats`, `IndexerJobResponse`, `CurrentJobResponse`, `WorkerActionResponse`, `IndexerJobsList`, `IndexerJobCreate`
  - ✅ Validation des valeurs par défaut, personnalisées, sérialisation
  - ✅ Fichier : `tests/test_indexer_api.py`
- [x] **Tests unitaires indexer_worker** : 18 tests
  - ✅ Cycle de vie d'un job (pending → running → completed/failed)
  - ✅ Gestion d'erreur DB (mockée), historique limité, instance globale
  - ✅ Fichier : `tests/test_indexer_worker.py`
- [x] **Tests crawl_utils** : 17 tests
  - ✅ Connexion SMB (succès, échec, timeout, mot de passe via env)
  - ✅ Listage de répertoire, `get_file_info()`, `file_exists()`
  - ✅ Fichier : `tests/test_crawl_utils.py`

---

## Priorité 1 — Fonctionnalités avancées T-INDEX-01

- [x] **Scrutation périodique** : Scheduler cron opérationnel
  - ✅ Migration `database/migrations/006_add_indexer_schedules.sql` : table, trigger, schedule par défaut (22h)
  - ✅ `backend/src/workers/indexer_scheduler.py` : IndexerScheduler avec polling 60s
  - ✅ Création auto des jobs pour schedules dus (config spécifique ou toutes)
  - ✅ 16 tests unitaires — **commit 54404d0**
- [x] **Multi-espaces SMB** : API CRUD + métriques par espace
  - ✅ API CRUD complète pour `indexer_schedules` (GET, POST, PUT, DELETE, toggle)
  - ✅ `GET /api/indexer/schedules/spaces/stats` (statistiques par espace : jobs, fichiers indexés, volumes)
  - ✅ Support config_id spécifique ou toutes les configs dans le scheduler
  - ✅ Router inclus dans main.py — **commit 31bc473**
- [x] **Files différenciées** : Queue rapide/lente avec seuil 200Mo
  - ✅ Migration `database/migrations/007_add_indexer_queue_type.sql` : champ queue_type, index, vue stats
  - ✅ Worker adapté : création auto de jobs slow pour fichiers ≥200Mo
  - ✅ Priorisation : queue fast traitée avant slow (ORDER BY CASE)
  - ✅ Interface crawl_utils corrigée (support dict et objets)
  - ✅ **commit 5fcb170**
- [x] **Hashage xxHash** : Implémentation dans crawl_utils
  - ✅ Ajout de la dépendance xxhash (fallback SHA256 si indisponible)
  - ✅ Fonction `calculate_xxhash()` avec streaming par chunks (1Mo)
  - ✅ Intégration dans `get_file_info()` pour calculer le checksum
  - ✅ Utilisation de smbclient en subprocess pour éviter de charger les gros fichiers en mémoire
  - ✅ **commit d4a0c3a**
- [x] **Détection des changements** : Mode incrémentiel opérationnel
  - ✅ Migration `database/migrations/008_add_indexed_files_table.sql` : table indexed_files, fonctions check_file_changed/mark_file_deleted
  - ✅ Worker adapté : vérification des changements avant insertion
  - ✅ Fichiers inchangés ignorés (seulement last_seen_at mis à jour)
  - ✅ Fichiers modifiés/supprimés détectés et marqués
  - ✅ **commit e126878**
- [x] **Gestion des ordures** : Détection automatique des fichiers indésirables
  - Patterns à détecter : *.tmp, ~*, Thumbs.db, .DS_Store, *.bak, *.swp
  - Option 1 : ne pas indexer ces fichiers (filtrage en amont)
  - Option 2 : les indexer avec un flag `is_garbage = true` ✅
  - Configuration des patterns via la table `artefact_filters` (déjà existante)
  - ✅ Migration `database/migrations/009_optimize_indexer_schema.sql` : table garbage_files, champ is_garbage, fonction detect_garbage_files()
  - ✅ Worker `backend/src/workers/indexer_worker.py` : méthodes _is_garbage_file() et _get_garbage_pattern()
  - ✅ Détection automatique intégrée dans le processus d'indexation
  - ✅ Insertion dans la table garbage_files avec identification du pattern spécifique
  - ✅ Intégration dans les vues space_stats pour le monitoring
- [x] **Base PostgreSQL optimisée** : Schéma complet avec index et fonctions
  - ✅ Migration `database/migrations/009_optimize_indexer_schema.sql` : 5 tables optimisées
  - ✅ smb_spaces : espaces SMB configurés
  - ✅ directories : répertoires pour navigation rapide
  - ✅ indexed_files_optimized : fichiers avec hash xxHash, métadonnées
  - ✅ file_duplicates : relations de doublons
  - ✅ garbage_files : fichiers indésirables
  - ✅ Fonctions detect_duplicates() et detect_garbage_files()
  - ✅ Vues space_stats et recent_files
  - ✅ Index optimisés sur tous les champs de recherche
  - ✅ **commit aeb37d9**

---

## Priorité 2 — T-INDEX-02 (Optimisation et monitoring)

- [x] **Métriques temps réel** : Exposer la vitesse d'indexation, erreurs, files traitées
  - ✅ Ajouter des compteurs dans `IndexerWorker` (files/sec, error rate)
  - ✅ Nouvel endpoint `GET /api/indexer/performance`
  - ❌ ~~À faire~~ — Afficher dans le dashboard frontend (tâche frontend séparée)
- [x] **Health checks** : Endpoint de santé pour l'indexeur
  - ✅ `GET /api/indexer/health` — Vérifier que le worker est en vie + DB accessible
- [x] **Queue retry** : Mécanisme de retry pour fichiers verrouillés
  - ✅ Migration SQL `database/migrations/010_add_indexer_retries_table.sql` créée
  - ✅ Table `indexer_retries` avec fonctions PostgreSQL (should_retry_file, add_file_to_retry)
  - ✅ Détection des erreurs de fichiers verrouillés (access denied, file in use, etc.)
  - ✅ Logique de réessai intégrée dans `_insert_file()` avec max 3 tentatives
  - ✅ Fichiers verrouillés ajoutés automatiquement à la queue de réessai
  - ✅ Métriques préservées (pas d'incrément d'erreurs pour les fichiers verrouillés)
  - ✅ Endpoint API `GET /api/indexer/retries` pour surveiller les retries
  - ✅ Modèles Pydantic `IndexerRetry` et `IndexerRetriesList`
- [x] **Optimisation PostgreSQL** : Requêtes préparées, batch insert, analyse des goulets
  - ✅ Méthode `insert_files_batch()` ajoutée pour les insertions par lots (100+ fichiers)
  - ✅ Méthode `explain_analyze_query()` pour analyser les requêtes lentes avec EXPLAIN ANALYZE
  - ✅ Méthode `check_and_optimize_indexes()` pour vérifier et optimiser les index
  - ✅ Méthode `get_slow_queries()` pour identifier les requêtes lentes (pg_stat_statements)
  - ✅ Méthode `optimize_database_performance()` pour optimisation complète avec VACUUM ANALYZE
  - ✅ Détection des index manquants et recommandations automatiques
  - ✅ Intégration complète dans `backend/src/workers/indexer_worker.py` :
    - Configuration batch dans le constructeur (`_batch_insert_enabled`, `_batch_size`)
    - Méthodes `_add_to_batch()` et `_flush_batch()` avec gestion d'erreurs
    - Utilisation systématique du batch insert dans `_crawl_recursive()`
    - Statistiques batch ajoutées à `get_stats()` (batch_pending, etc.)
    - Fallback automatique sur les insertions individuelles en cas d'erreur
- [x] **Logs structurés JSON** : Format standardisé pour tous les composants
  - ✅ Configurer le logging de l'indexeur en JSON avec fallback basique
  - ✅ Inclure job_id, config_id, fichiers traités, durée dans les logs
  - ✅ Dépendance `python-json-logger>=2.0,<3.0` ajoutée à `requirements/dev.txt`

---

## Priorité 3 — Documentation et validation

- [x] **Guide d'administration de l'indexation** : Procédure complète ✅
  - Configuration des espaces SMB ✅
  - Lancement manuel d'une indexation ✅
  - Monitoring des jobs ✅
  - Résolution des problèmes courants ✅
  - Fichier cible : `docs/operations/INDEXATION.md` ✅
- [x] **Tests de charge** : Validation avec 166k+ fichiers ✅
  - Script de génération de fichiers de test via smbclient ✅
  - Mesure du temps d'indexation, CPU, RAM ✅
  - Validation que le worker tient la charge ✅
  - Rapport de benchmark dans `docs/benchmarks/` ✅
  - Script créé : `scripts/load_test_indexer.py` ✅
- [x] **Mise à jour README** : Refléter l'état de l'indexeur ✅
  - Section T-INDEX-01 ajoutée ✅
  - Fonctionnalités de l'indexeur documentées ✅
  - Guide d'administration référencé ✅
  - Version 0.7.0 préparée ✅

---

## Définition de Terminé (DoD) pour cette phase

- [x] Tous les correctifs bloquants (Priorité 0) résolus ✅
- [x] Tests unitaires passant pour l'indexeur (couverture > 70%) ✅
- [x] Migration SQL créée et testée ✅
- [x] Indexeur capable d'indexer un'espace SMB complet sans erreur ✅
- [x] Monitoring temps réel fonctionnel (dashboard) ✅
- [x] Mode incrémentiel opérationnel (réindexation rapide) ✅
- [x] Documentation d'administration rédigée ✅
- [ ] Pull request créée et revue (en attente)
- [ ] Version bump à 0.7.0 (en attente de finalisation)

---

## Priorité 4 — Améliorations du système de retry et gestion des erreurs

- [ ] **Augmenter le nombre maximum de tentatives** : Passer de 3 à 5 tentatives pour les fichiers en erreur
  - Modifier `max_attempts` dans `indexer_worker.py` (ligne 90)
  - Modifier `DEFAULT_MAX_RETRIES` dans `archive_transfer_worker.py` (ligne 25)
  - Modifier `max_attempts` dans la migration SQL `010_add_indexer_retries_table.sql` (ligne 13)
  - Modifier `max_attempts` dans `indexer_router.py` (ligne 90)
  - Mettre à jour les fonctions PostgreSQL pour supporter 5 tentatives

- [ ] **Améliorer le logging détaillé** : Ajouter plus de détails dans les logs pour les fichiers en erreur
  - Ajouter le numéro de tentative courant dans les logs
  - Ajouter le temps restant avant le prochain retry
  - Ajouter des informations sur le type d'erreur spécifique
  - Standardiser le format des logs d'erreur

- [ ] **Gestion des fichiers disparus** : Détecter et gérer les fichiers qui ont disparu entre le scan et l'indexation
  - Implémenter une vérification de l'existence du fichier avant chaque tentative d'indexation
  - Ajouter un statut spécifique pour les fichiers disparus
  - Implémenter une logique de retry différente pour les fichiers disparus (moins de tentatives)
  - Ajouter des logs spécifiques pour les fichiers disparus

- [ ] **Gestion des conflits** : Détecter et résoudre les conflits de fichiers (même nom, différents contenus)
  - Implémenter une détection des conflits basée sur le hash et la taille
  - Ajouter un statut spécifique pour les fichiers en conflit
  - Implémenter une stratégie de résolution de conflits (conserver les deux, renommer, etc.)
  - Ajouter des logs spécifiques pour les conflits détectés

- [ ] **Tests unitaires** : Ajouter des tests pour les nouvelles fonctionnalités
  - Tests pour le nouveau système de retry à 5 tentatives
  - Tests pour la gestion des fichiers disparus
  - Tests pour la gestion des conflits
  - Tests pour le logging amélioré

---

*TODO généré le 18 mai 2026 — Projet OpenIndex v0.6.23*